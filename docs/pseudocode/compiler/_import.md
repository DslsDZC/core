# 共享引入文件（_import.cr）伪代码
> 源文件：src/compiler/_import.cr（35 行）
> 功能概要：编译器模块共享引入文件。src/compiler/ 目录下所有源文件自动引入本文件中的全部依赖模块，实现跨文件符号可见性。

## 标识符对照表
本文件为纯引入文件，不含函数定义。

## 全局状态
本文件不声明全局变量，仅包含引入语句。

## 说明
本文件是（src/compiler/）目录的「共享引入」文件，列出了编译器各模块所需的全部外部依赖。引入的模块包括命令行参数解析、语法树节点定义与全局常量、全局变量声明、动态数组读写工具、词法分析器、语法分析器、类型检查器、诊断信息输出、IR 指令生成、数据流图构建、二进制序列化、导入解析与文件标识符管理、（TOML）配置解析、项目级配置加载、操作系统接口、字符串格式化、输入输出、IR 解释器、调试转储工具、泛型单态化、增量编译缓存、扩展管理器、扩展安全检查、AST 级优化遍、IR 级优化、指针分析、区域检查、来源验证、运行时全局变量、编译器主入口与管线编排、程序入口点。

本文件无函数定义，不建函数节。
