# _import.cr 伪代码

> 源文件：src/stdlib/_import.cr（3 行）
> 功能概要：纯引入文件，为所有使用 Core 标准库的 .cr 源文件提供共享引入。当前仅引入 fmt 模块（字符串格式化与转换工具）。

## 标识符对照表

本文件为纯引入文件，不包含任何函数定义或变量声明。

## 全局状态

无。

本文件仅包含一行 `import fmt`，使 `src/stdlib/` 下的其他 .cr 文件无需各自引入 fmt 模块即可使用字符串长度（str_len）、整数转字符串（int_str）等公共函数。
